// DrinksSection is now handled by KitsSection with filterCategory="bebidas"
// This component is kept for backward compatibility but delegates to KitsSection

const DrinksSection = () => null;

export default DrinksSection;
